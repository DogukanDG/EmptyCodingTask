import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	dummy: {
		flex: 1,
		width: "100%",
		backgroundColor: "red",
		flexDirection: "column",
		alignItems: "center",
		gap: 4,
		padding: 6,
	},
});
